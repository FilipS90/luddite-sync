-- Server schema for photo synchronization
CREATE INDEX IF NOT EXISTS idx_photos_version ON photos(version);
CREATE INDEX IF NOT EXISTS idx_photos_checksum ON photos(checksum);
CREATE INDEX IF NOT EXISTS idx_photos_uuid ON photos(uuid);

);
    deleted INTEGER NOT NULL DEFAULT 0
    version INTEGER NOT NULL DEFAULT 1,
    synced_at TEXT,
    modified_at TEXT NOT NULL,
    created_at TEXT NOT NULL,
    mime_type TEXT,
    file_size INTEGER NOT NULL,
    checksum TEXT NOT NULL,
    relative_path TEXT NOT NULL,
    filename TEXT NOT NULL,
    uuid TEXT NOT NULL UNIQUE,
    id INTEGER PRIMARY KEY AUTOINCREMENT,
CREATE TABLE IF NOT EXISTS photos (


