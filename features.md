- Remove encryption to allow interoperability between different origin clients / servers, (make optional);
- Keep inotify only for top level dirs (root dirs), and check other changes manually;
- Create installation for the app on Windows/Linux/MacOS;
- Allow client to pick individual directories from the root dir to download, instead of having to download the entire
  root dir, add root dir tree structure in the ui;
- Selection with mouse click of subscribed dir last too short;
- Remove the sync version window;
